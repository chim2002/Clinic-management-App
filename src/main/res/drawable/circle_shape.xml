<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">
    <solid android:color="@color/green" /> <!-- Circle color -->
    <size
        android:width="152dp"
        android:height="156dp" /> <!-- Circle size -->
    <stroke
        android:color="@color/popup"
        android:width="2dp" />
</shape>
